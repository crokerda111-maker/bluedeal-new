# Optional: Coupang lptag (example)
# COUPANG_LPTAG=YOUR_TAG_HERE

# Optional: Price API base URL (VPS)
# 기본 가정(현재 운영): 카페24 VPS에서 가격 JSON을 서빙
# PRICE_API_BASE=https://blusedeal.co.kr
#
# VPS는 아래 엔드포인트를 제공하면 됩니다.
# - GET /meta.json
# - GET /prices/{category}.json
#
# 참고) 나중에 인프라를 옮겨도(예: 다른 VPS, 오브젝트 스토리지 등)
# PRICE_API_BASE만 바꾸면 프론트는 그대로 동작하게 설계되어 있습니다.

# Online posts storage (recommended: Vercel KV / Upstash Redis REST)
# KV_REST_API_URL=https://<your-upstash-endpoint>
# KV_REST_API_TOKEN=<your-token>
